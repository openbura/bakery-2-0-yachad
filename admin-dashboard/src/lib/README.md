# Future Supabase boundary

Stage A intentionally contains no client, network calls, credentials or environment reads.

In Stage B, the Bakery-only Supabase browser client will be created here after the dedicated project, Auth model and RLS policies are explicitly approved.
